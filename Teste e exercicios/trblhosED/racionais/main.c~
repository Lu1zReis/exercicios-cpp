#include <stdio.h>
#include "racionais.h"

int main () {
	Tq n1, n2;
	n1.num = 2;
	n1.den = 3;
	n2.num = 5;
	n2.den = 2;
	printf("%d/%d\n", sum(n1,n2).num, sum(n1,n2).den);
	printf("%d/%d\n", sub(n1,n2).num, sub(n1,n2).den);
	return 0;
}
